# Runtime volumes written by Pi-hole
pihole/etc-pihole/*
!pihole/etc-pihole/.gitkeep

# Tor persistent state (keys, circuits, cache)
tor/tor-data/*
!tor/tor-data/.gitkeep

# Support bundles and other diagnostics
support/
pi_support_*.tar.gz
systemd/pi-tor.env

# Local environment overrides / secrets
.env

# OS cruft
.DS_Store
Thumbs.db
